# Netlify deployment for the Next.js storefront.
# The official plugin handles SSR, ISR, image optimization and API routes.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
